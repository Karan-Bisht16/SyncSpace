// Styling for PostForm.jsx
const styles = () => ({
    clearBtn: {
        "&:hover": {
            backgroundColor: "button.secondary",
            color: "button.primary"
        }
    },
    postBtn: {
        bgcolor: "#0090c1",
        color: "#ffffff",
        marginLeft: "8px",
        "&:hover": {
            backgroundColor: "button.secondary",
            color: "button.primary"
        }
    }
});

export default styles;